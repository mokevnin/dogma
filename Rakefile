require "bundler/gem_tasks"

#require 'dogma'
#require 'bundler/setup'
#require 'sequel'

#namespace :db do
  #namespace :sqlite do
    #desc "create sqlite database"
    #task :create do
    #end
  #end
#end
